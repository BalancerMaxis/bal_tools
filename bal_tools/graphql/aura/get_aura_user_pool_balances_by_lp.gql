query AuraUserPoolBalancesByLp($lpToken: String!, $block: Block_height) {
  pools(where: { lpToken: $lpToken }, block: { number: block }) {
    accounts(where: { staked_gt: 0 }) {
      account {
        id
      }
      staked
    }
  }
}
